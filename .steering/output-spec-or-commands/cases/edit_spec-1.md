# edit_spec-1

- **メソッド**: edit_spec
- **期待形式**: spec
- **実出力分類**: spec
- **サブラベル**: (該当なし)
- **一致**: True
- **記録日時**: 2026-07-10T23:22:39

## 入力プロンプト

以下の現行 spec を、追加要望に応じて更新し、更新後の spec 全文（Markdown・実現方針/変更範囲/手順）を返してください。
--- 現行spec ---
# spec: クーポン機能

## 実現方針
カート画面にクーポンコード入力欄を追加し、入力されたコードを検証して合計金額に割引を適用する。

## 変更範囲
- cart.html: クーポン入力欄の追加
- cart.js: クーポン検証・割引計算ロジックの追加
- coupon_service.py: クーポンコードの検証API

## 手順
1. cart.html にクーポン入力欄と適用ボタンを追加する
2. cart.js に適用ボタンのイベントハンドラを追加する
3. coupon_service.py にクーポンコード検証関数を実装する
4. 割引適用後の合計金額をカート画面に反映する
--- 追加要望 ---
クーポンに有効期限を設定できるようにしたい。期限切れのクーポンはエラーメッセージを表示して適用しない。


## 出力全文

## 実現方針
カート画面にクーポンコード入力欄を追加し、入力されたコードを検証して合計金額に割引を適用する。クーポンコード検証時には有効期限もチェックし、期限切れのクーポンは適用せず、エラーメッセージを表示する。

## 変更範囲
- cart.html: クーポン入力欄の追加
- cart.js: クーポン検証・割引計算ロジック、有効期限切れエラー表示ロジックの追加
- coupon_service.py: クーポンコードの検証API、有効期限チェックロジックの追加

## 手順
1. cart.html にクーポン入力欄と適用ボタンを追加する
2. cart.js に適用ボタンのイベントハンドラを追加する
3. coupon_service.py にクーポンコード検証関数と有効期限チェックロジックを実装する
4. 割引適用後の合計金額をカート画面に反映する
5. 期限切れのクーポンが入力された場合、cart.js でエラーメッセージをユーザーに表示する
